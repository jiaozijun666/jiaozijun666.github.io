# 个人主页使用说明

这是 `jiaozijun666.github.io` 的全新源码，基于 al-folio 主题重建。旧版（academicpages）的内容已经全部迁过来，模板自带的示例页面全部清掉了。

---

## 一、先做这一步，否则网站不会更新！

旧站是 GitHub Pages 直接渲染 master 分支；新站不行，必须让 GitHub Actions 先构建。

1. 把本文件夹的全部内容推到仓库 `jiaozijun666/jiaozijun666.github.io` 的 `master` 分支。建议先给旧内容打个 tag，或者开一个 `backup-old-site` 分支，方便回滚。
2. 推送后 `.github/workflows/deploy.yml` 会自动运行，把构建结果推到 `gh-pages` 分支（第一次大约 3 到 5 分钟）。
3. 打开仓库 **Settings → Pages → Build and deployment**，把 Source 改成 **Deploy from a branch → `gh-pages` → `/ (root)`**，保存。

以后每次 push 到 master 都会自动重新构建，不需要再动这个设置。

---

## 二、想改内容，改这几个文件就够了

| 想改什么 | 改哪里 |
| --- | --- |
| 首页那段自我介绍、头像下的一行定位、Education 两行 | `_pages/about.md` |
| 首页的 news 列表 | `_news/` 目录，一条一个文件，命名 `YYYY-MM-DD-短名字.md`，照着 `_TEMPLATE.md.example` 写 |
| Research 页的六个条目 | `_projects/` 目录，一个项目一个文件。`importance` 控制同组内排序，`category` 决定归到 research experience 还是 coursework projects |
| CV 页的全部内容 | `_data/cv.yml` |
| 邮箱、GitHub、LinkedIn、CV 下载链接 | `_data/socials.yml` |
| 网站标题、描述、关键词、favicon | `_config.yml` 最上面几行 |
| 图片 | `assets/img/` |
| CV 的 PDF | `assets/pdf/CV_Zijun_Jiao.pdf`。它是用 `cv-source/Ziyun_Jiao_CV.tex` 编译出来的，**改了 .tex 要重新编译并覆盖这个 PDF**，同时 `_data/cv.yml` 里的网页版 CV 也要同步改 |

**`_data/cv.yml` 有个坑**：CV 模板只认识固定的 section 名（Experience、Education、Projects、Skills、Languages 等）。改成别的名字（比如 Research Experience）**不会报错，但那一整块会渲染成空白卡片**。文件里也写了这条注释。

`_sass/_custom.scss` 是排版微调（卡片样式、字号、CV 左侧那一列等）。主题自带的样式一行没动，所以以后升级 al-folio 不会冲突。不想动排版就不用碰它。

---

## 三、本地预览（可选）

装了 Docker 的话，在本文件夹里执行：

```bash
docker compose up
```

然后打开 http://localhost:8080/ 。

装了 Ruby 的话：

```bash
bundle install
bundle exec jekyll serve
```

然后打开 http://localhost:4000/ 。

---

## 四、上线前后建议补的东西

1. **换一张头像**。现在用的是那张傍晚的照片，我裁成了方形，但背景偏暗、偏旅拍。找一面浅色墙拍一张正装半身照就够，换掉 `assets/img/prof_pic.jpg` 即可（正方形，800px 以上）。
2. **时序 benchmark 和 ESG 这两个项目，补上你本人负责的部分**。铁路项目和 SCG 已经写了个人贡献，这两个没写，放在一起看会显得刻意，面试也大概率会问。确定之后 `_projects/2_timeseries.md`、`_projects/3_esg.md` 和 `_data/cv.yml` 三处一起改。
3. **news 现在只有四条，全是学历事件**。以后有 arXiv、开源、比赛结果、投稿，随手加一条。访客判断一个主页是不是"活的"，基本只看 news 的最新日期。
4. **LinkedIn** 资料填好后，在 `_data/socials.yml` 里取消那一行注释。Google Scholar 先别挂，点进去是空的比没有更差。
5. **学校 logo**：CV 页 Education 左边现在是 THU / NUS / SOTON 三个文字小方块。如果能拿到三张校徽 PNG，放进 `assets/img/logos/`，再把 `_sass/_custom.scss` 末尾那三行 `content: "THU"` 之类换成 `background-image` 就行，位置和尺寸都留好了。

---

## 五、页面上写了什么，需要你确认

主页和 CV 上关于你的所有描述都是按你的 CV（`cv-source/Ziyun_Jiao_CV.tex`）写的，包括：

- 清华：The Future Laboratory，导师 Prof. Haipeng Mi，Research Assistant（non-degree appointment），2025.06 至今
- 铁路项目写的是你负责的三块：后端构建与外部 tool agent 集成、会议纪要的两层 agent 工作流、四分区权限工作区与委托契约
- 每个团队项目都注明了你本人的角色，SCG 明确写了推导与实验是其他同学做的
- 三处"边界说明"：选股系统没做 backtesting、ESG 的人工标注与外部评级比对属于后续验证阶段、本科毕业论文是理论综述没有原创数值实验

这些都是有意保留的，招生委员会看到"主动说明方法边界"通常算加分项。但**如果哪一条与事实不符，一定要先告诉 Leo 改掉，不要直接上线**。
